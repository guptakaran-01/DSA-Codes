class Solution {
    public int repeatedNTimes(int[] nums) {
     int n=nums.length;
     HashSet<Integer> m=new HashSet<>();
     for(int i=0 ; i<n ; i++){
        if(m.contains(nums[i])) return nums[i];
        m.add(nums[i]);
     }   
     return -1;
    }
}